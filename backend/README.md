# BetPlus backend (FastAPI)

Quick scaffold for local development.

Run with Docker Compose:

```bash
docker compose up --build
```

The API will be available at `http://localhost:8000` and a health check at `/health`.

Environment variables are read from `DATABASE_URL` and `SECRET_KEY`.
